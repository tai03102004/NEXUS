import abi from "./Transactions.json";

export const contractABI = abi.abi;
export const contractAddress = "0x..."; // Địa chỉ contract sau khi triển khai
// npx hardhat run scripts/deploy.js --network ropsten
